- content_for(:title) { 'Home' }

- if @topic
  %h1 News

  - @threads.each do |thread|
    - post = @news_posts[thread]

    %hr

    .mb-4
      = render 'forums/posts/news_post', post: post

  - if @more_threads
    .row.justify-content-center.my-5
      = link_to 'View Older', forums_topic_path(@topic), class: 'btn btn-primary'
- else
  .jumbotron
    %h1 Welcome to the Respawn League website

    %p.- Oh no you shouldn't be seeing this!
    %hr
    %p Report this to us via our #{link_to 'Discord', 'https://discord.rsl.tf/'}
